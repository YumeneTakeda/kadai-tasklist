<% if logged_in? %>
    <p>ログイン完了ユーザ:<%= current_user.name %></p>
    <h1>タスク一覧</h1>
<ul>
    <% @task.each do |task| %>	

<li><%= link_to task.id, task %> : <%= task.status%> &gt <%= task.content %></li>
    <% end %>
</ul>

<%= paginate @task %>
aaa
<%= link_to '新規タスク', new_task_path %>

<% else %>
    <h1>Welcome to the Tasklists</h1>
    <%= link_to "Sign up now !", signup_path %>
<% end %>

